% To build a coloured revision diff between main_old.tex (previous submission)
% and main.tex (current revision), run:
%
%   ./make_diff.sh
%
% from this directory. That script runs latexdiff with the flags that keep
% both added and deleted content inside PNAS's \matmethods{} block (which
% otherwise silently loses one side of the diff), and then compiles
% main-d.tex to main-d.pdf via latexmk.
